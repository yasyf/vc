<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>VoteCollector - <%= yield(:title) %></title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <script src="//cdnjs.cloudflare.com/ajax/libs/jQuery-linkify/1.1.7/jquery.linkify.js"></script>
  <!--[if lt IE 9]>
    <script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
    <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
  <![endif]-->
  <%= csrf_meta_tags %>
</head>
<body>
  <%= nav_bar fixed: :top, brand: 'VoteCollector' do %>
    <% if current_user %>
      <%= menu_group pull: :right do %>
        <%= drop_down "<span id='user_drop' class=#{'inactive_user' unless current_user.active?}>#{current_user.name}</span>" do %>
          <%= menu_item current_user.active? ? 'Go Inactive' : 'Go Active',
              toggle_active_api_v1_user_path, remote: true, method: :post, id: 'toggle_active' %>
        <% end %>
      <% end %>
    <% end %>
  <% end %>
  <div class="container">
    <div class="body">
      <%= bootstrap_flash %>
      <% if content_for?(:header) %>
        <h1> <%= yield(:header) %></h1>
      <% end %>
      <%= yield %>
    </div>
  </div>
</body>
</html>
